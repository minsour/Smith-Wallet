import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  listSectionContainer: {
    width: '100%',
    height: '100%',
    margin: 5,
    marginLeft: -5,
  },
  valueText: {
    marginTop: 10,
  },
});
